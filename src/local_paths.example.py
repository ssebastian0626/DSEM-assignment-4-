"""Copy this file to local_paths.py (same folder) if your raw/processed data lives
somewhere other than <repo>/data/raw and <repo>/data/processed - e.g. a different drive
or a shared folder. local_paths.py is gitignored, so your paths stay local to your machine.

If you don't need this - your data is already at <repo>/data/raw - don't create
local_paths.py at all; the defaults in paths.py will be used.
"""

RAW = r"C:\path\to\your\data\raw"
PROCESSED = r"C:\path\to\your\data\processed"
